import { StyleSheet } from "react-native";

const styleAlbumCover = StyleSheet.create({
    imageContainer: {
        padding: 20,
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
        // backgroundColor: '#00c6b1',
    },
    titleImage: {
        width: 300,
        height: 300,
        // backgroundColor: '#953c1c',
    },
});

export default styleAlbumCover;