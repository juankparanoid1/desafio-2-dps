import { StyleSheet } from "react-native";

const stylePlayerControls = StyleSheet.create({
    controls: {
        paddingVertical: 20,
        paddingVertical: 20,
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'space-evenly',
    },
});

export default stylePlayerControls;