import { StyleSheet } from "react-native";

const stylePlayerSlider = StyleSheet.create({
    playerControlsSlider: {
        paddingVertical: 20,
        justifyContent: 'center',
        alignItems: 'center',
    }
});

export default stylePlayerSlider;