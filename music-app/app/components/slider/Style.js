import { StyleSheet } from "react-native";

const styleSlider = StyleSheet.create({
    musicContainer: {
        // backgroundColor: '#2e2191',
        flexDirection: 'row',
        alignItems: 'center',
        // paddingHorizontal: 15,
        paddingBottom: 15,
    },
    sliderContainer: {
        height: 160,
        // backgroundColor: '#009204',
    },
    arrow: {
        // paddingHorizontal: 5,
    },
    sliderTitleContainer: {
        paddingBottom: 5,
    },
    sliderTitle: {
        fontSize: 17,
    },
});

export default styleSlider;