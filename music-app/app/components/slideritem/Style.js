import { StyleSheet } from "react-native";

const styleSliderItem = StyleSheet.create({
    sliderItem: {
        width: 160,
        justifyContent: 'center',
        alignItems: 'center'
    },
    sliderInfoContainer: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        // backgroundColor: '#1623d5'
    },
    sliderInfoTitle: {
    }
})

export default styleSliderItem;